import "../admin.css";

export const metadata = {
  title: "Prijava v administracijo",
  robots: { index: false, follow: false },
};

export default function PrijavaLayout({ children }) {
  return children;
}
